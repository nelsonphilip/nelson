#include<stdio.h>
#include<conio.h>
void main()
{
    int a;
    printf("  Enter the num");
    scanf("%d",&a);
    if(a!=0)
    {
        a=a+1;
    }
    printf( "the next num %d",a);
    getch();
}
